<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pizzaria Fantástica</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
    <form>
        <input type="text" name="busca" placeholder="Qual pizza você procura?" autocomplete="off">
        <button type="submit">Buscar</button>
    </form>
    <main>
        <% for (let pizza of pizzas) { %>
            <article>
                <img src="<%= pizza.img %>" alt="<%= pizza.nome %>">
                <a href="/pizzas/<%= pizza.id %>"><button>+ Add </button></a>
                <div>
                    <%= pizza.nome %>
                </div>
                <span>
                <%= pizza.preco.toLocaleString('pt-br', { minimumFractionDigits: 2 , style: 'currency', currency: 'BRL' }) %></span>
            </article>
            <% } %>
    </main>
</body>

</html>